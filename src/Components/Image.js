import styled from "styled-components";
import heroMobile from '../images/hero-mobile.jpg';

const Image = styled.div`

`;

export default () => {
    return (
        <Image>
            <img src={heroMobile} alt="woman" />
        </Image>
    )
}